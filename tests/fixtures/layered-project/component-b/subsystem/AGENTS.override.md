# Subsystem override instructions

- Marker: `subsystem-override`.
- This override is the effective source for its directory and takes normal precedence over a same-directory standard instruction file or configured fallback.
- Investigate locally and do not treat the override itself as a blocker.
