# Component A instructions

- Marker: `component-a`.
- These instructions add to `fixture-root` for targets under this component.
- Keep changes inside this component and use its public function as the owner boundary.
